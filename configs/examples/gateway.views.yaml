views:
  # Example gateway launch with keyring auth:
  #   mcp-gateway --mounts configs/examples/gateway.mounts.yaml --views configs/examples/gateway.views.yaml --auth-keys ./auth-keys.json
  # Views are the easiest way to scope capability tokens (allow specific view names).
  - name: notes_recent
    tool: files.search
    args:
      mount: notes
      query: "hello"
      max_results: 10
      max_bytes: 20000

  # - name: notes_sql
  #   tool: sqlite.query
  #   args:
  #     mount: notes
  #     path: "notes.db"
  #     query: "SELECT id, title FROM notes LIMIT 50"
  #     max_rows: 50
  #     max_bytes: 50000

  # - name: signed_asset_cc
  #   tool: c2pa.inspect
  #   args:
  #     mount: notes
  #     path: "signed.jpg"
  #     trust: "off"

  # - name: repo_recent_commits
  #   tool: git.log
  #   args:
  #     mount: notes
  #     repo: "."
  #     max_commits: 20
  #     max_bytes: 120000
  #     max_lines: 2000

  # - name: repo_status
  #   tool: git.status
  #   args:
  #     mount: notes
  #     repo: "."
  #     porcelain: true
